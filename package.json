{
  "name": "daggerdice",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy",
    "dev:worker": "wrangler dev",
    "verify": "curl -s -o /dev/null -w \"%{http_code}\" https://daggerdice.chris-personal-account-072.workers.dev"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.5.0",
    "typescript": "~5.8.3",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "@3d-dice/dice-box": "^1.1.4",
    "alpinejs": "^3.14.9"
  }
}
